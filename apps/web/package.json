{
  "name": "web",
  "version": "1.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve web",
    "build": "ng build web",
    "watch": "ng build web --watch --configuration development",
    "test": "ng test web",
    "lint": "ng lint web",
    "dev": "ng build web --watch --configuration development",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "private": true
}